services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: moodlog
      POSTGRES_PASSWORD: moodlog
      POSTGRES_DB: moodlog
    ports:
      - "5432:5432"
    volumes:
      - moodlog-db:/var/lib/postgresql/data

volumes:
  moodlog-db:
